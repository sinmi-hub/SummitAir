"""Phase 4 external integrations: Google Sheets (CRM) and Google Calendar (demos).

Both sit behind small interfaces so the backing store can be swapped later
(e.g. a SettlLeadStore) without touching the brain or pipeline.
"""
